# config.json 
```json
{
  "vits": {
    "url": "vits语音接口",
    "uid": "qq号码",
    "token": "token值",
    "modelId": "模型ID",
    "roleId": "角色ID",
    "ar": 0.5, // 情感数值，默认不动
    "domin": 0.5, // 情感数值，默认不动
    "va": 0.5 // 情感数值，默认不动
  },
  "gpt": {
    "ruleType": "规则类型"
  }
}